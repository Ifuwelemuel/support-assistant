# Learning log
One entry per day. Honest. This becomes interview material and textbook draft.

## Day 1 — 2026-09-24

**Did:** stopped conda auto-activating; installed current git and gh via
Homebrew; SSH auth to GitHub; created support-assistant with protected main
and squash-only merges; first PR through the full loop.

**Understood for the first time:** (write it — e.g. why git 2.15 had no
`git switch`; that a branch is a pointer; what the index is for)

**Confused by:** (write it — the confusion is the useful part)

**Would do differently:**

**Commands I want to remember:** `git add -p`, `git switch -c`,
`git rebase -i main`, `git check-ignore -v`, `git reflog`